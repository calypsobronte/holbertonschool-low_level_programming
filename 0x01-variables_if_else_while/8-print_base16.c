#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c;
char d;
for (c = 48; c < 58; c++)
putchar(c);
for (d = 97; d < 103; d++)
putchar(d);
putchar('\n');
return (0);
}
