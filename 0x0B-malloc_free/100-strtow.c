#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * strtow - h
 * @str: point en string
 * Return: NULL
*/
char **strtow(char *str)
{
char **a;
int i, k;
if (str == NULL)
return (NULL);
if (*str == '\0')
return (NULL);
}
