#include "holberton.h"
/**
 * main - empty file
 *
 * Return: ALways 0 (Success)
 */
int _islower(int c)
{

if (c > 'a' && c < 'z')
{
return (1);
_putchar('\n');
}
else
{
return (0);
_putchar('\n');
}
}

